.DS_Store
venv/
__pycache__/
*.pyc
.env
node_modules/
dist/
.demo-logs/
.demo-pids
# redis-server writes its snapshot into whatever directory it was launched from,
# which is the repo root when Redis is started for a local demo.
dump.rdb
